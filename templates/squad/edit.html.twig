{% extends 'basev2.html.twig' %}

{% block body_id 'squad_edit' %}

{% block title %}Edition de Squad{% endblock %}

{% block contentHeaderTitle %}
    <h1 class="m-0">Edition de Squad</h1>
{% endblock %}


{% block contextual_menu %}
    {% if is_granted('ROLE_MANAGER') %}
        <li class="nav-item d-none d-sm-inline-block">
            <a href="{{ path('squad_index') }}" class="nav-link"><i class="fas fa-th-list"></i> Retour à la liste</a>
        </li>
    {% endif %}
{% endblock %}

{% block body %}
    {{ form_start(form) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary">
            <i class="fa fa-save" aria-hidden="true"></i> {{ 'action.save'|trans }}
        </button>
    {{ form_end(form) }}
{% endblock %}